model:
  name: Qwen2.5-0.5B
  max_length: 2048

training:
  batch_size: 1
  learning_rate: 2e-4
  num_epochs: 3
  accumulation_steps: 16
  warmup_steps: 100

paths:
  data_dir: ./data
  output_dir: ./output
  cache_dir: ~/.cache/huggingface